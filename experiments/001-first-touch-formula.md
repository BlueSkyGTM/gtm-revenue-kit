# 001 — First-touch copy: PVP or the fixed-slot template?

- **Instruments:** `foundations/pvp.md` (upstream: strip the CTA, the message must still
  teach) vs Michael's fixed-slot template (`motions/tam/skeleton.md` §copy: plain,
  never-rewritten, only the value prop and the list are ever tested)
- **Hypothesis:** neither is wrong — they optimize different regimes. Expected shape of
  the answer: fixed-slot for the broad market (yield discovery at volume), PVP for the
  researched head of the list. That tier split is itself the thing to test, not to assume.
- **Runs in:** the albatross deployment's first campaigns — fenton as client account #1
  (the service-model dry run). Each campaign declares its instrument in its brief.
- **Decision gate:** same list quality, same offer, instrument as the variable; reply
  rate and positive-reply rate over enough sends that the difference is not noise. The
  account's scoring model states the numbers it will trust before the test starts.
- **Status:** queued
- **Verdict:** —
