# Formal request to the second brain — reconstruction inputs

**From:** the construction session (Claude Code, branch
`claude/course-extraction-account-split-lm15br`)
**To:** the Cowork second-brain workspace pointed at this repo
**Standing:** this file is transient — it is deleted when its requests are fulfilled or
superseded. Each request states what it unblocks and what "fulfilled" looks like.

**How to respond:** commit files to this repo (any branch; name it `cowork/<topic>` if
you want isolation, or commit straight onto this branch). Rough notes are acceptable
everywhere — the construction session shapes them to `foundations/task-craft.md`. State
provenance per item (Michael / your own improvement / external source with citation):
provenance is the admission rule (`foundations/principles.md` §1), so an item without it
cannot enter core. **Anything not committed to this repo does not exist for the
construction session.**

---

## Tier 1 — blocks construction (nothing else proceeds meaningfully without these)

### R1. The improved TAM framework, stage by stage

The centerpiece. For each stage — Michael's six are the placeholder list in
`motions/tam/CONTEXT.md` (mapping · refinement · find & enrich · copywriting & testing ·
AI personalization · deliverability & send) — supply:

- the method as you now run it, and **what you changed from Michael's version and why**
- worked examples where they exist (real ones can be cited abstractly; the swap test
  applies to core, but examples can live in an account or be anonymized)
- any numbers, marked as: your proven values / Michael's taught values / guesses

**Also state whether the six-stage list itself survives your improvement** — if your
framework restructures the stages, the pipeline folders follow your structure, not
Michael's. *Fulfilled when:* stage content exists in-repo for at least stage 1, and the
stage list is confirmed or replaced. Stages can land one at a time; each becomes a
numbered pipeline folder on arrival.

### R2. The revenue-engineering definition and rubric

The working definition you are drafting — frameworks and methodology — even partial.
It becomes the admission rubric for everything after it, and `estate.md`'s claim that
"GTM motions are the connective layer that prevents waste" is currently *my* rendering
of your words. Two specific gaps it should close:

- **The waste taxonomy:** which wastes, between which systems? (attention → offer,
  offer → funnel, funnel → delivery, …) This becomes the admission test's sharp edge:
  *what waste does this method prevent?*
- **Whether "revenue engineering" subsumes or sits beside the GTM kit** — it decides
  whether this repo eventually renames/re-scopes or stays one machine in a larger estate.

*Fulfilled when:* a definition doc exists in-repo (location: `foundations/`, name your
choice) and `estate.md` §"What this kit is" is corrected against it.

---

## Tier 2 — unblocks specific queued work

### R3. Michael's signal set, with backstory

`signals/` is deliberately empty: no "why it matters," no entry (`signals/schema.md`).
The plays carry their own signals' mechanisms, but Michael covers signals beyond the
plays. For each signal worth keeping: trigger · what fires · **why it matters (the
buying mechanism)** · how to track · fast/slow decay. *Fulfilled when:* records exist
and the shelf stops being empty for admitted signals.

### R4. Verdict on the four-dimension split (Fit / Intent / Relationship / Timing)

`signals/schema.md` adopts this decomposition from `icp-intelligence-mcp` (MIT,
`NOTICE.md`). You pointed at that repo's signal layer — confirm this is the layer you
meant, and adopt / modify / replace the dimension set. *Fulfilled when:* a one-line
verdict lands (an edit to `schema.md` or a note).

### R5. The fixed-slot template, verbatim

`experiments/001` tests PVP against Michael's cold-email formula, but
`motions/tam/skeleton.md` §copy holds only my abstraction of the formula. A fair test
needs the real instrument: **Michael's template text as taught** (slots and all).
*Fulfilled when:* the verbatim template is in-repo with attribution, and the skeleton's
§copy points at it.

### R6. Evaluation of the build itself

The six judgment calls to check hardest, in order of risk:

1. `foundations/principles.md` — six files of rules compressed to one page. What got lost?
2. The standards split — PVP/why-now/verify-specifics kept as law; gates and benchmarks
   demoted to account-template defaults marked unverifiable. Right call per number?
3. The empty signal shelf (vs. migrating the old set with retro-fitted backstories).
4. `motions/tam/` gated on your brief (vs. me drafting stages you then correct).
5. The archive scope (`_archive/2026-08-13-framework/README.md` maps it) — anything
   retired that should come back?
6. The five-form assignments (umbrella / knowledge bundle / pipeline / record libraries /
   context-map seed).

*Fulfilled when:* verdicts arrive per item — "stands" is a complete verdict.

---

## Tier 3 — research requests (standing; fulfill opportunistically)

### R7. Michael's source material — location and preservation

No git repo holds the course (verified across all four repos' full history). Even though
we build from your improved framework, backstory restoration and provenance checks need
the source reachable. **State where the handbook, playbook, and 6-part TAM series
physically live**, and ideally commit a copy (or the durable pointer) somewhere durable —
your call where, given licensing.

### R8. Pain-based segmentation — firm up experiment 002's instrument

The recorded source is one Cannonball GTM / Doug Bell piece. Before 002 runs: is there
more (follow-ups, competing EDP formulations, anyone publishing results)? You called it
a work in progress that people predict will overwrite firmographic frameworks — the
research question is whether the prediction has evidence yet, and what the strongest
current formulation of the method is. *Deliverable:* notes into
`motions/workflows/pain-based-segmentation.md` or an accompanying research file.

### R9. Benchmarks with provenance

The retired core benchmarks (reply-rate tables, funnel diagnostics) had no known source.
Accounts now set their own (`_template/context/scoring-model.md` §8). If you want
defaults back: supply benchmark figures **with sources you trust**, or declare the kit
runs benchmark-free until its own campaign data lands. Either answer closes this.

### R10. The estate inventory (albatross phase — parked until you say)

The funnel builder, branding tools, copywriting material, and SKU definitions, when you
unpack them — for the estate dissection (`estate.md` names the squatters waiting on
this). Includes: whether ClickFunnels is the funnel machine of record (its connector
exists on the construction session, unauthorized).

---

## Coordination

- **Interchange is git.** Commit; the construction session fetches. Nothing else
  persists between us.
- **Live channel (optional):** connect Remote Control on the machine running Cowork and
  the construction session can message the workspace directly (`ListAgents` currently
  shows no reachable agents).
- **Sequencing:** R1/R2 gate the pipeline build. R3–R6 land in any order. R7–R10 are
  standing. The construction session holds: re-pointing, shaping to task-craft, walk
  tests, and the upstream-baseline diff (`baseline-gtm-starter-kit`) for provenance
  measurement.
